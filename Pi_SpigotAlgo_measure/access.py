# Given an local IP address, it will help to connect the device through SSH.

targetData = {
    "ip": "192.168.1.195",
    "os": "w10",
    "user": "benja",
    "password": "benja1212"
}

#- Script that enables the ssh connection of the windows device.
    # The script will be executed on the device.


#- Check if the target is vulnerable to the attack.
#- If the target is vulnerable, then connect to the target and execute the attack.
    #- Connect to the target device through SSH.
    #- Scan ports

#- (Auxiliar func) Detect local IP address
    # f'sudo nmap -sP {hostname}/24'


